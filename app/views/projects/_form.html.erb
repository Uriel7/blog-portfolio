<%= form_for @project do |f| %>
  <% if @project.errors.any? %>
    <h2> <%= pluralize(@project.errors.count, "error")%> impiden guardar este project:</h2>
    <ul>
      <% @project.errors.full_message.each do |msg| %>
        <li> <%= msg %>
      <% end %>
    </ul>
    <% end %>
    <%= f.label :title %><br>
    <%= f.text_field :title %>

    <br>
    <br>

    <%= f.label :description, "Descripción" %><br>
    <%= f.text_area :description %>

    <br>
    <br>

    <%= f.label :link, "Link" %><br>
    <%= f.text_field :link %>

    <%= f.submit %>
<% end %>